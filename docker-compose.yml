services:
  birdnet-frame:
    image: ghcr.io/icecoldfire/birdnet-frame:main
    container_name: birdnet-frame
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ${HOST_DATA_DIR:-./data}:/app/data
